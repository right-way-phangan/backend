node_modules/
.pgdata/
.env
.env.local
dist/
*.log

.DS_Store
.vercel

# api/index.js is the esbuild bundle (the Vercel function). It is COMMITTED so
# git auto-deploy has the function present without relying on Vercel to run the
# build (Vercel classifies this layout as static and skips it). `npm run build`
# regenerates it; rebuild + commit when src/api changes.
.pgdata-smoke/

# worktree symlink, never commit
/node_modules

# iCloud (fileproviderd) плодит рядом с папками пустые дубликаты «X 2».
# 2026-08-24 такая папка попала под `git add -A` в web: git счёл это
# переименованием и увёл все 76 фотографий застройщика в несуществующий
# каталог. В индекс их не пускаем.
* 2
* 2.*
