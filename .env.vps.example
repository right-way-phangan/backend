# VPS stack env (docker compose --env-file .env.vps). Do NOT commit the real file.
POSTGRES_PASSWORD=change-me-strong
API_TOKEN=change-me-long-random        # web (OBJECTS_API_TOKEN) + bot must match
API_DOMAIN=api.rightwaygroup.co        # A-record → VPS IP; Caddy auto-TLS

# For in-container migrations (migrate:amocrm / migrate:leads):
AMOCRM_DOMAIN=rightwaykpg.amocrm.ru
AMOCRM_TOKEN=
AMOCRM_OBJECTS_CATALOG_ID=9077
